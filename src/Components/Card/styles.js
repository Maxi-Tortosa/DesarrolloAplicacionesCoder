import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	container: {
		margin: 50,
		width: 300,

		borderWidth: 1,
		borderColor: 'lightgrey',
		borderRadius: 10,
		display: 'flex',
		alignItems: 'center',
	},
	name: { fontWeight: '700' },
});
