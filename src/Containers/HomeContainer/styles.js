import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
	container: {
		flex: 1,
		display: 'flex',
		alignItems: 'center',
		// justifyContent: 'center',
	},

	searcherInput: {
		fontSize: 15,
		marginTop: 90,
		borderWidth: 1,
		elevation: 5,
	},
});
